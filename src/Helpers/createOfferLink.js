import randomString from "random-string";

const createOfferLink = (data) => {
    // create unique link id
    

    // register the link on backend
    // return the link
}

export default createOfferLink;